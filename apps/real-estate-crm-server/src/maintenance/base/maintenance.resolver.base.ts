/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Maintenance } from "./Maintenance";
import { MaintenanceCountArgs } from "./MaintenanceCountArgs";
import { MaintenanceFindManyArgs } from "./MaintenanceFindManyArgs";
import { MaintenanceFindUniqueArgs } from "./MaintenanceFindUniqueArgs";
import { DeleteMaintenanceArgs } from "./DeleteMaintenanceArgs";
import { MaintenanceService } from "../maintenance.service";
@graphql.Resolver(() => Maintenance)
export class MaintenanceResolverBase {
  constructor(protected readonly service: MaintenanceService) {}

  async _maintenancesMeta(
    @graphql.Args() args: MaintenanceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Maintenance])
  async maintenances(
    @graphql.Args() args: MaintenanceFindManyArgs
  ): Promise<Maintenance[]> {
    return this.service.maintenances(args);
  }

  @graphql.Query(() => Maintenance, { nullable: true })
  async maintenance(
    @graphql.Args() args: MaintenanceFindUniqueArgs
  ): Promise<Maintenance | null> {
    const result = await this.service.maintenance(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Maintenance)
  async deleteMaintenance(
    @graphql.Args() args: DeleteMaintenanceArgs
  ): Promise<Maintenance | null> {
    try {
      return await this.service.deleteMaintenance(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
