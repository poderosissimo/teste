/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Fornecedor } from "./Fornecedor";
import { FornecedorCountArgs } from "./FornecedorCountArgs";
import { FornecedorFindManyArgs } from "./FornecedorFindManyArgs";
import { FornecedorFindUniqueArgs } from "./FornecedorFindUniqueArgs";
import { DeleteFornecedorArgs } from "./DeleteFornecedorArgs";
import { FornecedorService } from "../fornecedor.service";
@graphql.Resolver(() => Fornecedor)
export class FornecedorResolverBase {
  constructor(protected readonly service: FornecedorService) {}

  async _fornecedorsMeta(
    @graphql.Args() args: FornecedorCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Fornecedor])
  async fornecedors(
    @graphql.Args() args: FornecedorFindManyArgs
  ): Promise<Fornecedor[]> {
    return this.service.fornecedors(args);
  }

  @graphql.Query(() => Fornecedor, { nullable: true })
  async fornecedor(
    @graphql.Args() args: FornecedorFindUniqueArgs
  ): Promise<Fornecedor | null> {
    const result = await this.service.fornecedor(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Fornecedor)
  async deleteFornecedor(
    @graphql.Args() args: DeleteFornecedorArgs
  ): Promise<Fornecedor | null> {
    try {
      return await this.service.deleteFornecedor(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
