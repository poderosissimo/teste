/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Ideia } from "./Ideia";
import { IdeiaCountArgs } from "./IdeiaCountArgs";
import { IdeiaFindManyArgs } from "./IdeiaFindManyArgs";
import { IdeiaFindUniqueArgs } from "./IdeiaFindUniqueArgs";
import { DeleteIdeiaArgs } from "./DeleteIdeiaArgs";
import { IdeiaService } from "../ideia.service";
@graphql.Resolver(() => Ideia)
export class IdeiaResolverBase {
  constructor(protected readonly service: IdeiaService) {}

  async _ideiasMeta(
    @graphql.Args() args: IdeiaCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Ideia])
  async ideias(@graphql.Args() args: IdeiaFindManyArgs): Promise<Ideia[]> {
    return this.service.ideias(args);
  }

  @graphql.Query(() => Ideia, { nullable: true })
  async ideia(
    @graphql.Args() args: IdeiaFindUniqueArgs
  ): Promise<Ideia | null> {
    const result = await this.service.ideia(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Ideia)
  async deleteIdeia(
    @graphql.Args() args: DeleteIdeiaArgs
  ): Promise<Ideia | null> {
    try {
      return await this.service.deleteIdeia(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
